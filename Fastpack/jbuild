(jbuild_version 1)

(library
 ((name        Fastpack)
  (public_name Fastpack)
  (libraries (FastpackResolver lwt.unix fileutils yojson containers str FlowParser))
  (flags (:standard "-open" "Containers"))
  (preprocess (pps (lwt.ppx)))))
