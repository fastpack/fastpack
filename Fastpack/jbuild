(jbuild_version 1)

(library
 ((name        Fastpack)
  (public_name Fastpack)
  (library_flags (-linkall))
  (libraries (
    FastpackUtil FastpackTranspiler
    base64 lwt.unix logs logs.cli logs.lwt logs.top fmt fileutils yojson containers str
    FlowParser
    re re.posix
    ppx_inline_test.runtime-lib))
  (flags (:standard "-open" "Containers"))
  (preprocess (pps (lwt.ppx ppx_expect ppx_driver.runner (-inline-test-lib Fastpack) )))
  ))
