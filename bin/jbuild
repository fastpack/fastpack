(jbuild_version 1)

(executable
 ((name fpack)
  (public_name fpack)
  (package Fastpack)
  (libraries (Fastpack FastpackResolver cmdliner lwt.unix flow_parser fileutils yojson))
  (preprocess (pps (lwt.ppx)))))
