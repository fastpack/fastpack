(jbuild_version 1)

(library
  ((name FastpackServer)
  (public_name FastpackServer)
  (libraries (FastpackUtil lwt cohttp cohttp-lwt-unix websocket-lwt websocket-lwt.cohttp))
  (preprocess (pps (lwt.ppx)))))
